<h1>Home Page</h1>

<p><a href="/stylist/new">Add A Stylist</a></p>



  @if(@Model.Count > 0)
  {
    @foreach(var stylist in Model)
    {
      <li><h2>@stylist.GetName() </h2><a href = "/edit">Edit</a> || <a href = "/delete">Delete</a></li>
    }
  }
